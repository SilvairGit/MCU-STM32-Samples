#ifndef PROVISIONING_H
#define PROVISIONING_H

#include <stdbool.h>

// Initialize provisioning module.
void Provisioning_Init(void);

// Check if provisioning module is initialized.
bool Provisioning_IsInitialized(void);

#endif    // PROVISIONING_H
